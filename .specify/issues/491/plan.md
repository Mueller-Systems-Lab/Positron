# Plan — Issue #491

1. Inventory the installed launcher, Quickstart, doctor, advanced Compose,
   readiness contract, adapter discovery, and documentation authority.
2. Add a small shell configuration helper with strict validation, protected
   XDG storage, token-file/hidden-prompt input, generated local secrets, and
   profile-specific Compose environment generation.
3. Extend the installer-generated launcher with explicit supervised lifecycle
   commands while preserving demo defaults and actionable conflict errors.
4. Add deterministic tests for the helper and installer fixture; update the
   installed-user docs and architecture map.
5. Run focused checks, full mandated checks where available, and record
   compatibility and known external acceptance limits in Issue #491.
