# Issue #480 — Exploration Efficiency Replication Plan

1. Record repository reality, #476/#478 boundaries, and fresh main qualification.
2. Freeze the candidate identity, partitions, arms, metrics, thresholds,
   validity taxonomy, and stopping rule.
3. Add only disposable research/evidence tooling and focused tests; never alter
   production exploration behavior or promotion authority.
4. Run exactly three neutral calibration tasks, derive and freeze the runtime
   envelope, then execute five health canaries and the separate workload gate.
5. Generate the new hidden holdout and attempt every A/B/C cell exactly once.
6. Run the negative control, compute the single frozen decision, and conduct
   architecture, security, and research reviews.
7. Run all applicable tests and static gates, publish redacted metadata-only
   evidence, commit and open the research PR.
8. Execute the visible headed Playwright suite last; if landed, freshly qualify
   post-merge main and record the final result.
