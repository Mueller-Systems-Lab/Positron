# Issue #487 — Positron v0.2.0 Release Preparation Evidence

**State:** preparation / release candidate; stable publication has not occurred in this evidence.
**Preparation base:** `a8c4f33896e4da4a7cce3081c5bde5f0d6c921e0` (`origin/main` at run start)

## Reality lock

- Primary workspace start HEAD: `e03454d45736732b61b658300014888b7213787a` on `positron/issue-464-source-validation`.
- Primary workspace contained unrelated modified `packages/control-plane/src/index.ts` and untracked `.opencode/backups/`, `.tmp/`, and `docs/release/ui-workflow-proof/`; preserved unchanged.
- Issue #487: OPEN.
- Latest stable release: `v0.1.0`.
- `v0.2.0` tag: absent; stable `v0.2.0` release: absent.
- Existing `v0.2.0-rc.1` tag/release is a separate draft prerelease and was not modified.

## Version contract

The 14 current product manifests, root lockfile metadata, internal workspace ranges, `packages/shared/src/constants.ts`, `start.sh`, and the local release packager were aligned to `0.2.0`. Historical docs/evidence and fake adapter fixture values remain unchanged by design.

- Version consistency: PASS.
- Lockfile regeneration: `npm install --package-lock-only --ignore-scripts` completed successfully.
- Dependency graph drift: none observed; lockfile diff is limited to product version metadata and internal workspace ranges.

## Preparation validation

| Gate | Result |
| --- | --- |
| `npm ci --ignore-scripts` | PASS; native binding rebuilt separately for local execution |
| Build | PASS |
| Typecheck | PASS |
| Format check | PASS (600 files) |
| Root tests | PASS — 2,733/2,733 (149 files) |
| Web tests | PASS — 422/422 (22 files) |
| Contract tests | PASS — 168/168 (5 files) |
| Integration tests | PASS — 20/20 |
| Route-smoke unit tests | PASS — 14/14 |
| Transfer references | PASS |
| Review execution | PASS |
| Install doctor demo JSON | PASS |
| Quickstart dry-run | PASS |
| Full repository lint | Existing advisory backlog; no release-file regression claim |
| Differential lint | Pending committed release head |

The first test attempt after `npm ci --ignore-scripts` failed only because `better-sqlite3` had no native binding; `npm rebuild better-sqlite3` passed and all repeated database-dependent suites passed. No product code fix was made for that environment issue.

## Feature-line audit

The v0.1.0-to-current history and closed issue evidence support the release-note claims for runtime-budget v1, operator-readiness v1, install-doctor v1, shared Quickstart first-build behavior, and general evidence-gated evaluation infrastructure. `PROGRESSIVE_LOCALIZATION_V1` was evaluated and rejected; exploration and skill specialization/productization are not activated.

## Safety

No npm publication, container publication, deployment, credential rotation, force-push, tag rewrite, or unsupervised Real Mode execution was performed. Fake/demo remains the default and push/merge safety gates remain enabled.
