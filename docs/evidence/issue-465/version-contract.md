# Version contract

`package.json` and workspace package manifests use `0.1.0`; this is the
authoritative development version. Existing `v0.1.0-rc.1` and `v0.2.0-rc.1`
tags are historical and no release/tag was created by this work. Changelog and
release documents require a final consistency pass before release.
