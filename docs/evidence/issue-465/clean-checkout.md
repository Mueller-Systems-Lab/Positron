# Clean checkout

`npm ci` completed from the committed `package-lock.json`; native
`better-sqlite3` was rebuilt using the repository-declared tooling. `npm run
build` passed. Node requirement is Node 22 (Dockerfiles and CI); package manager
is npm. No undeclared local dependency was used.
